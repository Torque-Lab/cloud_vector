{
  "name": "cloud_vector",
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.19",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "build:https-server": "turbo run build:prod --filter=@cloud/https-server",
    "build:infra-provisioner": "turbo run build:prod --filter=@cloud/infra-provisioner",
    "build:control-plane": "turbo run build:prod --filter=@cloud/control-plane",
    "build:frontend": "turbo run build:prod --filter=@cloud/frontend",
    "start:frontend": "cd apps/frontend &&  bun run start:prod",
    "start:https-server": "cd apps/https-server && bun run start:prod",
    "start:infra-provisioner": "cd apps/infra-provisioner && bun run start:prod",
    "start:control-plane": "cd apps/control-plane && bun run start:prod"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
